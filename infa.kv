<Inf_ob_Grid>
    GridLayout:
        size: root.width, root.height

        cols: 1
        rows: 3

        GridLayout:
            cols: 2
            rows: 4

            Label:
                text: "Введите N:"
                color: 0,0,0,1
                size_hint: 1, 0.3
                font_size: 40

            TextInput:
                multiline: False
                inpuut_filter: "float"
                size_hint: 1, 0.3
                font_size: 50
                padding_y: self.height / 2.0 - (self.line_height / 2.0) * len(self._lines) - 10, 0

            Label:
                text: "Введите i:"
                color: 0,0,0,1
                size_hint: 1, 0.3
                font_size: 40

            TextInput:
                multiline: False
                inpuut_filter: "float"
                size_hint: 1, 0.3
                font_size: 40
                padding_y: self.height / 2.0 - (self.line_height / 2.0) * len(self._lines) - 10, 0

            Label:
                text: "Введите I:"
                color: 0,0,0,1
                size_hint: 1, 0.3
                font_size: 40

            TextInput:
                multiline: False
                inpuut_filter: "float"
                size_hint: 1, 0.3
                font_size: 40
                padding_y: self.height / 2.0 - (self.line_height / 2.0) * len(self._lines) - 10, 0

            Label:
                text: "Введите K:"
                color: 0,0,0,1
                size_hint: 1, 0.3
                font_size: 40

            TextInput:
                multiline: False
                inpuut_filter: "float"
                size_hint: 1, 0.3
                font_size: 40
                padding_y: self.height / 2.0 - (self.line_height / 2.0) * len(self._lines) - 10, 0

        Label:
            text: "Сделано Арсением Стариковыv vk@sv_n_nr"
            color: 200, 200, 200, 1
            size_hint_y: 0.1